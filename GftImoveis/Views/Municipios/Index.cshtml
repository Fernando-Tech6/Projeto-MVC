@model IEnumerable<GftImoveis.Models.Municipio>

@{
    Layout = "_NovoLayout";
}


@{
    ViewData["Title"] = "Municípios";
}

<head>
    <link rel="stylesheet" href="~/css/modelo.css">
</head>


<h1>Municípios</h1>
<p>
    <a asp-action="Create" class="btn btn-outline-primary">Criar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EnderecoM)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EnderecoM.Nome)
            </td>
            <td>
               <a asp-action="Edit" asp-route-id="@item.MunicipioId" class="btn btn-outline-primary">Editar</a>
                <a asp-action="Details" asp-route-id="@item.MunicipioId" class="btn btn-outline-primary">Detalhes</a>
                <a asp-action="Delete" asp-route-id="@item.MunicipioId" class="btn btn-outline-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
